import BottomNavLayout from '@/components/bottomNav';

const AskLayout = ({ children }: { children: React.ReactNode }) => {
  return <BottomNavLayout>{children}</BottomNavLayout>;
};

export default AskLayout;
